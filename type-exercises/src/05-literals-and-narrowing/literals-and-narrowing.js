// What is the type of helloConst and worldConst?
var helloConst = "Hello";
var worldConst = "World";
if (typeof helloConst === typeof worldConst) {
    var willThisBeTrue = true;
    console.log(willThisBeTrue); // TODO: will this be true or false?
}
